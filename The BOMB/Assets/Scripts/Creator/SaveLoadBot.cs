using UnityEngine;
using System.Collections;

public class SaveLoadBot : MonoBehaviour {
	/*
	public void Load () {
		SaveBot.Hull = PlayerPrefs.GetInt ("Hull");
		SaveBot.Movement = PlayerPrefs.GetInt ("Movement");
		for (int a = 0; a < SaveBot.Weapons.Length; a++) {
			SaveBot.Weapons[a] =  PlayerPrefs.GetInt ("Weapon"+a);
		}
	}

	public void Save () {
		PlayerPrefs.SetInt ("Hull", SaveBot.Hull);
		PlayerPrefs.SetInt ("Movement", SaveBot.Movement);
		for (int a = 0; a < SaveBot.Weapons.Length; a++) {
			PlayerPrefs.SetInt ("Weapon"+a, SaveBot.Weapons[a]);
		}
		PlayerPrefs.Save ();
	}*/
}
