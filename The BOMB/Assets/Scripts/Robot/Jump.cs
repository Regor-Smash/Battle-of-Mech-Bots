using UnityEngine;
using System.Collections;

public class Jump : MonoBehaviour {
	/*
	public float jumpPower = 10000;
	Rigidbody hull;

	void Start (){
		hull = transform.parent.GetComponent<Rigidbody> ();
	}

	void Update () {
		if (Input.GetButtonDown ("Jump") && transform.GetComponentInParent <Grounded> ().ground == true) {
			hull.AddForce (0, jumpPower, 0, ForceMode.Impulse);
		}
	}
	*/
}
